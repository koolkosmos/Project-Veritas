# Project-Veritas
A controlled experiment that measures where and why a standard deepfake detector fails, using FaceForensics++ under distribution shifts (compression, manipulation type, frame sampling), without claiming SOTA detection.
